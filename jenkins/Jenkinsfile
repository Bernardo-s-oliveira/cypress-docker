pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'npm clean package -DskipTests=true'
            }
        }
        stage('test cypress') {
            steps {
                sh 'npm run cy:run'
            }
        }
    }
}